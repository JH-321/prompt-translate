[package]
name = "koen"
version = "0.2.0"
edition = "2021"
description = "Korean→English prompt compressor for LLM pipelines"
license = "MIT"

[dependencies]
libc = "0.2"
regex = "1"
serde_json = "1"

[profile.release]
strip = true
